# This file previously contained LLMClient (OpenRouter via raw requests) —
# a synchronous client that was never used in the production flow.
# It has been removed to eliminate dead code and confusion.
#
# The active LLM client is: app/services/llm_service.py (AsyncOpenAI)
