# This file previously contained OllamaClient — a local LLM client that was
# never used in the production flow. It has been removed to eliminate dead code
# and confusion about which LLM client is active.
#
# The active LLM client is: app/services/llm_service.py (AsyncOpenAI)
