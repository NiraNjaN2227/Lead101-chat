# ─────────────────────────────────────────────
# CON AI — Student Assistant API
# Requirements
# ─────────────────────────────────────────────

# Web framework
fastapi==0.129.0
uvicorn==0.40.0
starlette==0.52.1

# Templating
jinja2==3.1.6
markupsafe==3.0.3

# Data validation & settings
pydantic==2.12.5
pydantic-settings==2.8.1
python-dotenv==1.2.1

# LLM (OpenAI)
openai==2.21.0
httpx==0.28.1

# Rate limiting
slowapi==0.1.9
limits==5.8.0

# Error monitoring
sentry-sdk==2.53.0

# HTTP client (used in generate_students.py)
requests==2.32.5

# ─────────────────────────────────────────────
# Optional — uncomment to enable
# ─────────────────────────────────────────────

# Intent detection via embeddings (Phase 3 upgrade)
# sentence-transformers>=2.2.2

# Redis cache (set CACHE_TYPE=redis in .env to activate)
# redis>=5.0.0
